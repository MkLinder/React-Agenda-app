import { Box, Center } from "@chakra-ui/react"
import { LoginBox } from "../components/Login"

export const LoginPage = () => {
    return (
        <Center>
            <LoginBox/>
        </Center>
    )
}